<?php
// Configuración básica por defecto (puede ser sobreescrita por .env)
if (!defined('APP_ENV')) {
  define('APP_ENV', 'development');
}
if (!defined('APP_DEBUG')) {
  define('APP_DEBUG', true);
}
if (!defined('APP_URL')) {
  define('APP_URL', 'http://localhost/edupro');
}

?>
